import http from 'http'; import https from 'https'; import url from 'url'; const PORT = process.env.PORT || 3000; const server = http.createServer((req, res) => { const query = url.parse(req.url, true).query; const target = query.url; if (!target) { res.writeHead(400); return res.end('URL não fornecida'); } const client = target.startsWith('https') ? https : http; client.get(target, (response) => { res.writeHead(response.statusCode, response.headers); response.pipe(res); }).on('error', (err) => { res.writeHead(500); res.end('Erro: ' + err.message); }); }); server.listen(PORT, () => { console.log('Proxy rodando na porta ' + PORT); });
